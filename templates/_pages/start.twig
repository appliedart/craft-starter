{% set possibleTemplates = ['_pages/' ~ entry.uri, '_channels/' ~ entry.section.handle ~ '-' ~ entry.type, '_channels/' ~ entry.section.handle, '_pages/default'] %}
{# {{ dump(possibleTemplates) }} #}
{% include possibleTemplates %}
